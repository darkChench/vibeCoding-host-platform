# 04 — 参数配置页（物模型 CRUD）

**What to build:** 参数配置页完整功能：左卡参数表（11 列：checkbox/参数名/显示名/地址/分类/类型/权限/单位/小数/范围/说明）+ 分类筛选下拉 + 设备地址输入框；右卡编辑表单（新增/编辑参数）+ 校验 + 保存/测试/取消。增删改查走状态层持久化（JSON 文件），校验规则（参数名唯一/地址合法 hex 且不重复/类型权限合法/min<=max/小数位>=0），未保存标记，全选三态。设备地址（1-247）更新协议层组帧用的 slave_id。

**Blocked by:** 02 — 侧边栏导航 + 页面路由, 03 — 协议层（参数表是协议层的数据源，slave_id 供协议层用）

**Status:** done

- [x] 参数表渲染 store.params，11 列完整
- [x] 新增参数：清空表单→填写→校验→保存追加到表
- [x] 编辑参数：勾选 1 条→载入表单→改→校验→保存更新
- [x] 删除参数：勾选≥1→二次确认→移除
- [x] 校验：参数名非空唯一、地址 0x0000-0xFFFF 不重复、min<=max、小数位>=0
- [x] 分类筛选下拉（全部/采样参数/配置参数），表格跟随筛选
- [x] 设备地址输入框（1-247），change 更新 store.slave_id
- [x] 全选三态（全选/部分/未选）
- [x] 持久化到 JSON 文件（`config/params.json`），重启不丢
- [x] 未保存标记（tag.warn）随修改显示
