# 02 — 侧边栏导航 + 页面路由

**What to build:** 点侧边栏树项（设备/数据/AI 三组），主区切换到对应页面，tabs 行联动显示当前页，active 态正确高亮。页面路由机制建立（类似原型 app.js showPage），各页面先用占位 widget，后续票逐个填充真实内容。

**Blocked by:** 01 — 工程骨架 + 主窗口框架

**Status:** done

- [x] 侧边栏 230px 固定宽，含三个分组（设备/数据/AI）的树导航
- [x] 每个树项点击 → 主区切换页面 + 标题栏副标题更新 + 状态栏当前页更新
- [x] tabs 行显示所有页面标签，当前页 active 态高亮
- [x] 页面路由用注册表（page_id → QWidget 工厂），新增页面只注册不改路由
- [x] 10 个页面占位 widget（显示页面名 + "待实现"），切换正常
- [x] 当前页状态保存在状态层，跨页面操作不丢失
